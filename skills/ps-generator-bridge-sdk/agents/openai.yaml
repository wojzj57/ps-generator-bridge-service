interface:
  display_name: "PS Bridge SDK"
  short_description: "Update the SDK protocol, client facade, or plugin devkit."
  default_prompt: "Use $ps-generator-bridge-sdk to update the SDK protocol, Connection facade, or plugin devkit."
